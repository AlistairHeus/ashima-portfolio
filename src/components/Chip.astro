---
/**
 * Bordered suggestion chip (Figma Suggestion chip).
 */
interface Props {
	readonly label: string;
	readonly muted?: boolean;
	readonly class?: string;
}

const { label, muted = false, class: className } = Astro.props;
---

<span
	class:list={[
		'type-chip inline-flex items-center justify-center rounded-lg border px-4 py-1.5 text-on-surface-variant',
		muted ? 'border-outline/80 opacity-50' : 'border-[#484848]',
		className,
	]}
>
	{label}
</span>
